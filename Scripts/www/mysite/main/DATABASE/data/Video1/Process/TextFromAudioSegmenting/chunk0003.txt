either has not been set so that speaks out here is nicely setting the so that's the basic problem which means that this should really be using the field but we can see here that this before class is static this field is not static so how did I try to instantiate in here this would have shown me as a syntax error and that's one thing that's very useful if you can code and with our syntax errors Point things out