but there's an important except let's try the same thing but with an object instead of a primitive like an here we see a Java point which is an object that simply stores two numbers that represent coordinates on the Cartesian plane the type of object doesn't matter I just chose this one because it's p255 and pass it into a function called change point which takes our Point p and changes it X and Y values 210 basically the same as